---
# Configuration Map for deployment.yaml
# Edit it to change the configuration of your proxy
# Don't forget to restart your proxy after changing it
#
# Path: examples/kubernetes/deployment.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: go-proxy-config
data:
  goproxy.yaml: |
    apiVersion: 1
    proxies:
    - namespace: example.org
      entries:
        repo: https://github.com/user/repo.git
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-proxy
  labels:
    app.kubernetes.io/name: go-proxy
    app.kubernetes.io/version: '1.0'
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: go-proxy
  template:
    metadata:
      labels:
        app.kubernetes.io/name: go-proxy
    spec:
      containers:
      - name: go-proxy
        image: atomys/go-proxy:1.0
        imagePullPolicy: IfNotPresent
        resources:
          requests:
            memory: "10Mi"
            cpu: "10m"
          limits:
            memory: "15Mi"
            cpu: "20m"
        ports:
        - containerPort: 8080
          name: http
        volumeMounts:
        - mountPath: /config/goproxy.yaml
          name: configuration
          subPath: goproxy.yaml
      volumes:
      - name: configuration
        configMap:
          name: go-proxy-config
---
apiVersion: v1
kind: Service
metadata:
  name: go-proxy
spec:
  selector:
    app.kubernetes.io/name: go-proxy
  ports:
  - port: 8080
    targetPort: 8080
